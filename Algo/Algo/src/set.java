public class set {
    protected int length = 10;
    protected Object[] Arr = {};
    protected int size = 0;
    protected set(int[] arr , int len){
        this.Arr = new Object[len];

        this.length = len;

        for(int i = 0 ; i < len ; i++){
            for(int o = i + 1 ; o < len ; o++){

            }
        }

        size = this.length;
    }
}
