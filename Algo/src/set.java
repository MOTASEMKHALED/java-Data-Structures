import Algo.AlgorithmsSort;
interface setMethod <S>{}

abstract class set<S> extends AlgorithmsSort<S>{
    private int l;
    private Object[] arr;
    protected set(int[] arr){
        this.Arr = new Object[length];

        this.length = length;

        for(int i = 0 ; i < length ; i++){
            for(int o = i + 1 ; o < length ; o++){

            }
        }

        size = this.length;
    }
}
